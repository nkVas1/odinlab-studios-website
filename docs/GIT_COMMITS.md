# 📖 Git Commit Guide для OdinLab Studios

Этот файл описывает правила создания хороших коммитов на русском языке.

## 🎯 Формат коммита

```
<тип>(<область>): <описание на русском> | <описание на английском>

<подробное объяснение (опционально)>

<footer - закрывающие issues (опционально)>
```

## 📋 Типы коммитов

| Тип | Описание | Эмодзи | Пример |
|-----|---------|--------|--------|
| `feat` | Новая функция | ✨ | `feat(hero): добавлена анимация морфинга` |
| `fix` | Исправление бага | 🐛 | `fix(3d): исправлена ошибка рендеринга` |
| `docs` | Обновление документации | 📚 | `docs(readme): добавлены инструкции` |
| `style` | Форматирование кода (без изменения функциональности) | 🎨 | `style: исправлено форматирование CSS` |
| `refactor` | Рефакторинг кода | ♻️ | `refactor(components): переструктурированы UI компоненты` |
| `perf` | Улучшение производительности | ⚡ | `perf(images): оптимизированы размеры изображений` |
| `test` | Добавление или обновление тестов | ✅ | `test(utils): добавлены unit тесты` |
| `chore` | Обновление зависимостей, конфигов (без кода продукта) | 🔧 | `chore(deps): обновлены зависимости Next.js` |
| `ci` | Изменения в CI/CD конфиге | 🔨 | `ci(github): добавлен workflow для Vercel` |

## 📍 Области коммитов

Основные области (в `<область>`):

- `hero` - Hero секция / главная страница
- `header` - Навигация и хедер
- `footer` - Подвал
- `components` - UI компоненты
- `3d` или `canvas` - Three.js и 3D элементы
- `animations` - GSAP анимации
- `services` - Секция услуг
- `contacts` - Форма контактов
- `styles` - Стили и Tailwind
- `seo` - SEO оптимизация
- `config` - Конфигурационные файлы
- `docs` - Документация
- `deps` - Зависимости

## ✅ Примеры хороших коммитов

### 1. Новая функция (feat)

```bash
git commit -m "feat(hero): добавлена анимация входа 3D объекта | Add 3D object entrance animation"
```

### 2. Исправление бага (fix)

```bash
git commit -m "fix(3d): исправлена ошибка рендеринга на мобильных | Fix rendering error on mobile devices"
```

### 3. Обновление документации (docs)

```bash
git commit -m "docs(readme): добавлены инструкции по деплою | Add deployment instructions"
```

### 4. Рефакторинг (refactor)

```bash
git commit -m "refactor(components): улучшена структура UI компонентов | Improve UI component structure"
```

### 5. Оптимизация производительности (perf)

```bash
git commit -m "perf(images): оптимизированы размеры и сжаты изображения | Optimize and compress images"
```

### 6. Обновление зависимостей (chore)

```bash
git commit -m "chore(deps): обновлены React 19 и Next.js 15 | Update React 19 and Next.js 15"
```

### 7. Добавление тестов (test)

```bash
git commit -m "test(header): добавлены unit тесты для компонента Header | Add unit tests for Header component"
```

### 8. GitHub Actions (ci)

```bash
git commit -m "ci(vercel): добавлен workflow для автоматического деплоя | Add automatic deployment workflow"
```

## ❌ Примеры плохих коммитов

```bash
# ❌ Слишком общее описание
git commit -m "fix stuff"

# ❌ Без контекста (области)
git commit -m "updated code"

# ❌ Только латиница (в нашем проекте используем русский)
git commit -m "feat: add animation"

# ❌ Слишком длинное описание в одной строке
git commit -m "feat: added very long description of what I did with the animation and fixed some bugs and added new features"

# ❌ Без типа
git commit -m "добавлена новая анимация"
```

## 📝 Подробное объяснение (опционально)

Для сложных изменений добавьте объяснение:

```bash
git commit -m "refactor(components): переструктурированы UI компоненты | Restructure UI components

- Разделены большие компоненты на более мелкие
- Улучшена переиспользуемость кода
- Добавлены типы TypeScript для всех props
- Обновлены импорты в других компонентах

Closes #123"
```

## 🔗 Закрытие Issues

Если коммит закрывает Issue на GitHub:

```bash
git commit -m "fix(contacts): исправлена форма контактов | Fix contact form

Closes #42"
```

Или несколько Issues:

```bash
git commit -m "feat(seo): добавлена SEO оптимизация | Add SEO optimization

Closes #42, #43, #44"
```

## 🔄 Процесс коммита

### 1. Проверьте статус
```bash
git status
```

### 2. Добавьте изменения
```bash
# Все файлы
git add .

# Или конкретные файлы
git add src/components/Header.tsx
git add docs/README.md
```

### 3. Напишите коммит
```bash
git commit -m "feat(header): добавлен кастомный курсор | Add custom cursor"
```

### 4. Отправьте на GitHub
```bash
git push origin main
```

## 💡 Советы

1. **Один логический элемент = один коммит**
   - Не смешивайте несвязанные изменения
   - Каждый коммит должен быть "атомарным"

2. **Понятные описания**
   - Описывайте ЧТО вы сделали, не КАК
   - Пишите в повелительном наклонении: "добавьте", "исправьте"

3. **Используйте русский + английский**
   - Основное описание на русском
   - Перевод на английском после "|"
   - Для международных команд

4. **Проверяйте перед пушем**
   ```bash
   git log --oneline -5  # Проверьте последние 5 коммитов
   ```

5. **Если ошибка в коммит-сообщении**
   ```bash
   git commit --amend  # Отредактировать последний коммит
   git push -f origin main  # Переправить (ОСТОРОЖНО!)
   ```

## 🎯 Быстрый старт

### Разработка нового компонента
```bash
git add components/ui/NewComponent.tsx
git commit -m "feat(components): создан компонент NewComponent | Create NewComponent"
git push origin feature/new-component
```

### Исправление на production
```bash
git add src/pages/home.tsx
git commit -m "fix(home): исправлен баг с отступами | Fix padding bug"
git push origin main
```

### Обновление документации
```bash
git add docs/*.md
git commit -m "docs: обновлена документация | Update documentation"
git push origin main
```

---

**Спасибо за внимание к деталям! 🙏**

*Чистая история коммитов = чистый и понятный проект*
