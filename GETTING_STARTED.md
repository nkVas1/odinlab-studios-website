# 📚 OdinLab Studios Website - Полная инструкция

## 🎯 Что здесь находится

Это полная структура профессионального веб-проекта OdinLab Studios, готового к разработке, деплою на Vercel и SEO-оптимизации.

## 📋 Содержимое проекта

```
✅ Полная структура Next.js 15 проекта
✅ React компоненты (Header, Footer, Canvas, Sections)
✅ 3D сцена с Three.js + React Three Fiber
✅ GSAP анимации и скролл-эффекты
✅ Tailwind CSS стилизация
✅ TypeScript типизация
✅ SEO оптимизация (robots.txt, sitemap.xml, мета-теги)
✅ GitHub Actions workflows
✅ Vercel конфигурация
✅ Документация (Setup, Development, Design)
✅ Environment примеры
✅ Git workflow гайд
```

## 🚀 Быстрый старт (5 минут)

### 1️⃣ Установка зависимостей

```bash
# Перейти в папку проекта
cd odinlab-studios-website

# Установить зависимости
npm install

# ИЛИ запустить скрипт инициализации (Windows/macOS/Linux)
# Windows:
scripts/init.bat

# macOS/Linux:
bash scripts/init.sh
```

### 2️⃣ Локальная разработка

```bash
# Запустить сервер разработки
npm run dev

# Откройте браузер: http://localhost:3000
```

### 3️⃣ Production сборка

```bash
# Создать оптимизированную сборку
npm run build

# Протестировать production версию локально
npm start
```

## 📤 Пуш на GitHub

### Первый раз (если репозиторий только создан):

```bash
# 1. Инициализировать git (если ещё не сделано)
git init

# 2. Добавить remote репозиторий
git remote add origin https://github.com/YOUR_USERNAME/odinlab-studios-website.git

# 3. Создать первый коммит
git add .
git commit -m "feat(init): инициальная структура проекта | Initial project structure"

# 4. Создать main ветку и сделать первый push
git branch -M main
git push -u origin main
```

### Последующие обновления:

```bash
# 1. Проверить статус
git status

# 2. Добавить изменения
git add .

# 3. Сделать коммит (на русском с переводом)
git commit -m "feat(hero): добавлена анимация входа | Add entrance animation"

# 4. Отправить на GitHub
git push origin main
```

### Правила коммитов:

```
<тип>(<область>): <описание на русском> | <описание на английском>

Типы:
- feat    - новая функция
- fix     - исправление бага
- docs    - обновление документации
- style   - форматирование кода
- refactor - рефакторинг
- perf    - оптимизация производительности

Примеры:
✅ feat(components): добавлен кастомный курсор | Add custom cursor
✅ fix(3d): исправлена ошибка рендеринга | Fix rendering error
✅ docs(readme): обновлена инструкция | Update instructions
```

## 🌐 Деплой на Vercel

### Способ 1: Через GitHub (Рекомендуется - САМЫЙ ПРОСТОЙ!)

1. **Убедитесь, что код на GitHub:**
   ```bash
   git push origin main
   ```

2. **Откройте https://vercel.com**

3. **Нажмите "Import Project"**

4. **Выберите "Import Git Repository"**

5. **Выберите ваш репозиторий из списка**

6. **Нажмите "Import"** и готово! 🎉

**Vercel автоматически:**
- 🔄 Развернет сайт на производство
- 📱 Создаст Preview URLs для Pull Requests
- ⚡ Оптимизирует изображения
- 🔒 Включит HTTPS
- 📊 Активирует аналитику

**После каждого `git push main` сайт обновится автоматически!**

### Способ 2: Через Vercel CLI

```bash
# 1. Установить Vercel CLI
npm i -g vercel

# 2. Залогиниться
vercel login

# 3. Развернуть на production
vercel --prod
```

### Доступ к Production

После деплоя ваш сайт будет доступен по адресу:
```
https://odinlab-studios-website.vercel.app
```

Или с кастомным доменом (если подключите).

## ✨ Что дальше (следующие шаги)

### Этап 1: Визуальный контент (1-2 недели)

1. **Генерируйте изображения через Flux:**
   - Используйте промпты из `docs/DESIGN_GUIDE.md`
   - Скачивайте в папку `public/images/`

2. **Добавьте изображения в компоненты:**
   ```tsx
   <Image 
     src="/images/hero-main-visual.png"
     alt="Hero visual"
     width={1920}
     height={1080}
   />
   ```

3. **Проверьте на production:**
   ```bash
   npm run build && npm run start
   ```

### Этап 2: Доп. страницы (1-2 недели)

Создайте новые страницы:

```bash
# Структура
mkdir -p app/services
cat > app/services/page.tsx << 'EOF'
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Услуги | OdinLab Studios',
  description: 'Наши услуги: разработка, дизайн, маркетинг',
};

export default function Services() {
  return <main>Services page</main>;
}
EOF
```

Повторите для: `/production`, `/games`, `/portfolio`, `/blog`

### Этап 3: Функциональность (2-4 недели)

- Форма контактов с Email отправкой
- API интеграции
- CMS или Блог
- Система комментариев
- Онлайн-калькулятор стоимости

### Этап 4: Финальная оптимизация (1 неделя)

```bash
# Проверить производительность
npm run build

# Проверить типы
npx tsc --noEmit

# Проверить лиинтинг
npm run lint

# Проверить SEO
# Откройте https://pagespeed.web.dev
```

## 📊 Мониторинг Production

### Vercel Dashboard

Войдите на https://vercel.com/dashboard чтобы видеть:
- 📈 Analytics (посещаемость)
- ⚡ Performance (Core Web Vitals)
- 🔍 SEO оценки
- 🐛 Ошибки и логи
- 📱 Мобильная оптимизация

### Custom Domain

1. Купите домен (Reg.ru, GoDaddy, Namecheap)
2. На Vercel в Project Settings → Domains
3. Добавьте домен и следуйте инструкциям
4. Через 24 часа будет доступен https://yourdomain.com

## 🔐 Безопасность

### Перед Production:

```bash
# Проверьте что .env.local в .gitignore
cat .gitignore | grep .env

# Не коммитьте секреты!
# Используйте Environment Variables в Vercel Dashboard
```

## 📞 Получить помощь

**Документация в проекте:**
- `docs/SETUP_GUIDE.md` - Полная инструкция по установке
- `docs/DEVELOPMENT.md` - Git workflow и правила разработки
- `docs/DESIGN_GUIDE.md` - Дизайн система и цвета
- `docs/API.md` - Интеграции и API

**Ресурсы:**
- Next.js: https://nextjs.org/docs
- Vercel: https://vercel.com/docs
- Three.js: https://threejs.org/docs
- GSAP: https://gsap.com/docs

## 🎉 Вы готовы!

Теперь у вас есть:
✅ Полностью функциональный проект на GitHub
✅ Автоматический деплой на Vercel
✅ SEO-оптимизированный сайт
✅ 3D и анимации
✅ Мобильная версия
✅ Production-ready код

## 📈 Roadmap

- [x] Инициальная структура
- [x] Компоненты
- [ ] Добавить изображения
- [ ] Дополнительные страницы
- [ ] Contact form с email
- [ ] Блог или Portfolio
- [ ] Analytics и tracking
- [ ] Custom domain
- [ ] Масштабирование функциональности

---

**Вопросы?** Смотрите документацию в папке `docs/`

**Успехов в разработке!** 🚀

---

*OdinLab Studios | Дата создания: 11 декабря 2025*
